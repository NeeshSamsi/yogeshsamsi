import { config } from "@keystatic/core"

export default config({
  storage: {
    kind: "github",
    repo: {
      owner: "NeeshSamsi",
      name: "yogeshsamsi",
    },
  },

  singletons: {},

  collections: {},
})
